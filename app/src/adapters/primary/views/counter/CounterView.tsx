import Counter from "@components/Counter.tsx";

const CounterView = (): JSX.Element => {
    return (
        <div>
            <Counter/>
        </div>
    );
};

export const Component = CounterView;
